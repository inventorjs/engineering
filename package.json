{
  "name": "@inventorjs/engineering",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "publish:patch": "pnpm -r exec pnpm version patch && pnpm -r publish --access=public",
    "publish:minor": "pnpm -r exec pnpm version minor && pnpm -r publish --access=public",
    "publish:major": "pnpm -r exec pnpm version major && pnpm -r publish --access=public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
